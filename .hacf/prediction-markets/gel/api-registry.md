# API Registry (GEL-2)

Append-only catalog of every API endpoint the program exposes or consumes.
Each entry: endpoint, contract schema, dependency graph.

## Entries

<!-- gel-api-appender appends records here. -->
